type: agent
human_readable_id: anthropic/claude-sonnet-45-or--plan-gen
model_config: models/anthropic/claude-sonnet-45-or
scaffold_config: agent_scaffolds/plan_generation
# Use --n K at runner level to generate K plans (K separate runs)
# Temperature optional - defaults to model's default
# temperature: 1.0
